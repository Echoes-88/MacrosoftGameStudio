
  export type Country = {
    name: string,
    capital: string,
  }
  
  export type AnswerCounter = {
    badAnswers: number,
    goodAnswers: number,
  }

  export enum InfoText {
    WIN = 'Gagné !',
    LOOSE = 'Perdu'
}